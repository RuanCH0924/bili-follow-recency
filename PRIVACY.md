# 隐私政策 / Privacy Policy

**扩展名称**：B站关注摸鱼博主
**生效日期**：2026-09-20
**适用版本**：0.8.3 及以后

---

## 一句话摘要

本扩展**不收集、不上传、不共享**任何个人数据。所有数据只保存在你自己的浏览器本地，唯一的网络请求是直接发往 B 站官方接口。

---

## 一、我们如何处理数据

### 1.1 不收集的数据

本扩展**不收集**以下任何信息：

- 个人身份信息（姓名、邮箱、电话等）
- 浏览历史、访问过的网页
- 设备信息、IP 地址、地理位置
- 使用行为统计、崩溃日志、埋点数据

本扩展**不包含**任何分析 SDK、广告 SDK 或第三方追踪代码。

### 1.2 在本地读取与存储的数据

| 数据 | 用途 | 存储位置 | 是否离开本机 |
| --- | --- | --- | --- |
| B 站登录态 Cookie（`SESSDATA`、`buvid3`、`bili_jct`） | 调用 B 站接口时的身份鉴权 | 仅内存中临时读取，不落盘 | 仅随请求发送给 B 站官方接口 |
| 你填写的 B 站 UID | 记住要查询哪个账号的关注列表 | `chrome.storage.local`（本机） | 否 |
| 查询到的关注列表与最近更新时间 | 下次打开时秒出结果，避免重复请求 | `chrome.storage.local`（本机） | 否 |
| 界面偏好（是否显示异常项）与设置项（反风控开关、自动更新开关与频率等） | 记住你的显示与自动化习惯 | `chrome.storage.local`（本机） | 否 |

以上数据全部保存在**你自己的浏览器本地存储**中。你可以随时通过扩展头部的「清空本地缓存」按钮删除，或在卸载扩展时由浏览器自动清除。

### 1.3 网络请求

本扩展发出的**全部**网络请求都指向 B 站官方域名 `*.bilibili.com`：

| 接口 | 用途 |
| --- | --- |
| `/x/relation/followings` | 拉取你的关注列表 |
| `/x/polymer/web-dynamic/v1/feed/space` | 查询某个 UP 主的最近动态时间 |
| `/x/space/upstat` | 动态接口无数据时的降级查询 |
| `/x/frontend/finger/spi` | 浏览器缺少 `buvid4` 设备标识时申请一次（可在设置中关闭「设备指纹补全」） |
| `/x/relation/modify` | **仅在你主动点击「批量取关」时**调用，用于取消关注 |

> 上述请求默认只在**你主动点击查询**时发出。若你在设置页开启了「自动更新」或「自动重新查询失败项」，扩展会在后台按你设定的频率自动发起**同样的请求**，不会额外发送任何数据；两者默认关闭，可随时关闭。

**不存在任何指向作者服务器或第三方服务器的请求。** 本项目不含后端服务。

---

## 二、权限用途说明

| 权限 | 为什么需要 |
| --- | --- |
| `cookies` | 读取你已登录 B 站的 Cookie，用于调用需要登录态的 B 站接口。没有该权限则无法获取关注列表。 |
| `storage` | 在本机保存查询结果缓存、UID 与界面 / 功能偏好。 |
| `host_permissions: https://*.bilibili.com/*` | 允许扩展向 B 站官方接口发起上述请求。 |
| `sidePanel` | 支持在浏览器侧边栏中显示插件界面（Chrome/Edge 114+，低版本自动忽略）。 |
| `alarms` | 定时唤醒后台脚本执行「自动更新」（默认关闭）。不涉及任何数据读取，仅用于定时。 |

本扩展不请求任何与上述用途无关的权限。

---

## 三、数据共享与第三方

**我们不会向任何第三方出售、出租或共享你的数据。**

唯一的例外是：为完成你请求的功能，扩展会把必要的鉴权信息与请求参数发送给 **B 站官方接口**（`*.bilibili.com`）。这一过程等同于你在浏览器中正常访问 B 站页面，受[哔哩哔哩隐私政策](https://www.bilibili.com/blackboard/privacy-pc.html)约束。

---

## 四、数据安全

- 所有数据存储于浏览器提供的沙箱化本地存储中，遵循浏览器同源策略。
- 扩展不设置任何远程代码加载，所有逻辑均包含在已审核的安装包内。
- 登录态 Cookie 仅在发起请求时从浏览器读取并直接附加到请求头，不做额外持久化，也不写入日志。

---

## 五、未成年人保护

本扩展不面向未成年人单独提供服务，也不会有意收集未成年人的任何信息。

---

## 六、政策变更

若本政策发生实质性变更，我们会在项目仓库的 [CHANGELOG.md](CHANGELOG.md) 与本文件中更新，并修改文首的生效日期。继续使用即视为接受更新后的政策。

---

## 七、联系方式

如对本隐私政策有疑问，请通过以下方式联系：

- 提交 Issue：<https://github.com/RuanCH0924/bili-follow-recency/issues>
- 项目仓库：<https://github.com/RuanCH0924/bili-follow-recency>

---

<sub>本扩展为开源项目，采用 [MIT 协议](LICENSE) 发布。你可以自行审阅全部源代码以验证上述声明。</sub>
