# Changelog

本项目的所有重要变更都会记录在此文件中。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

### 计划中

- 后台定时轮询，关注的 UP 主更新时桌面通知
- 在 B 站关注列表页面直接注入「最近更新」时间列
- Firefox 适配

---

## [0.8.0] - 2026-09-16

针对 B 站风控返回 `-352 风控校验失败` 导致部分 UP 主查询失败的问题，新增三组反风控能力与一项并发数设置，并提供设置页交由用户按需取舍。

### 新增

- **设置页**：可逐项开关三组反风控能力、调整并发数，每项都标注「好处」与「代价」；修改即时保存，另有「恢复默认」。入口为 popup 顶部的齿轮按钮，也可从扩展管理页的「扩展程序选项」进入
- **关于区块**：设置页底部展示版本号（直接读取 manifest，无需手工维护）、作者、GitHub 项目地址与开源协议
- **组 1 · 请求节奏拟人化**（默认关闭）：请求间隔加入 0~80ms 随机抖动、每处理 30 个插入一次 1~3 秒随机停顿、单条失败重试改为 0.9~1.4 秒指数退避
- **并发数**（默认 3，可选 1~5）：同一时刻并行发出的请求数量，全量查询与「重新查询失败的 UP 主」共用该值。调高更快但更接近批量抓取特征，调低更稳但更慢
- **组 2 · 风控熔断与自动恢复**（默认关闭）：识别 `-352 / -412 / -509 / -799` 后暂停整个队列，冷却 30~60 秒并显示倒计时，随后自动继续；被拦截的条目会重试而非直接判失败；单次任务最多冷却 3 次，超过则中止并提示稍后重试
- **组 3 · 设备指纹补全**（默认开启）：请求同时携带 `buvid3` / `buvid4` / `b_nut`；浏览器没有 `buvid4` 时自动向 `/x/frontend/finger/spi` 申请一次并长期缓存复用

### 说明

- 四项默认值中**只有「设备指纹补全」默认开启** —— 它不增加耗时、不影响体验，属于纯收益。反风控的另外三项都会拖慢查询或让进度条长时间停留，需用户看过利弊说明后主动开启
- 并发数原为代码内写死的 5（拟人化开启时降到 3），现独立成用户可调设置项，读取时会夹到 1~5 的合法区间，手工改坏 storage 也不会导致异常

### 变更

- `biliFetch` 统一识别风控码并抛出独立的 `RiskControlError`，把「整体被限流」与「单条数据失败」区分开
- 后台任务状态新增 `kind`（`query` / `retry`）：popup 据此区分「后台查询中」与「正在重新查询失败的 UP 主」，重试也不再刷新「上次全量查询时间」
- `runTask` 支持 `retry` 模式：只挑缓存里「查询失败」的条目重查，逐条写回缓存，不再整体覆盖
- `bili_prefs` 改为**合并写入**：设置页与 popup 各写各的字段，互不覆盖
- 移除请求头里无效的 `User-Agent` / `Origin` / `Referer` —— 三者都是浏览器的禁止修改头（forbidden header），JS 设置会被静默忽略，保留只会造成误导

### 修复

- **熔断开启时会静默丢数据**：命中风控那一批里「已经成功」的条目，会在重试轮次中被整体丢弃（既不重试、不入结果，缓存也缺失）。实测 4 个 UP 主只产出 2 个。现改为按轮次累积结果、最后按原始批次顺序输出
- **批量取关的回归缺陷**：`modifyRelation` 引用了已被删除的 `UA` 常量，一旦走取关路径即抛 `ReferenceError: UA is not defined`
- **批量重试的结果大量丢失**：`patchCachedItem` 是「读 → 改 → 写」三步且中间有 `await`，原实现在 popup 里以 3 并发调用它，两条同时读到同一份旧快照时会把对方的修改覆盖掉（丢更新）。实测 3 并发写 9 条只有 3 条落盘，用户重试完 283 条失败项后重新打开仍显示 283。现在 background 用 Promise 链把所有缓存读改写排成队，保证每次修改都基于最新快照
- **关闭面板会中断批量重试**：重试原先是在 popup 里循环发 `refresh-one`，popup 一关循环即终止，且已完成的条目要等整批结束才落盘。现改为 background 任务（`runTask(mid, 'retry')`）：脱离 popup 独立跑，关面板不中断，重新打开能通过任务状态看到它仍在进行，每查完一条即写回缓存
- **「已查询 N 个关注」不随重试增长**：这个数字原先统计的是列表里的条目数（成功与失败都算），首次查询跑完就恒等于关注总数，所以批量重试把失败项救回来后它纹丝不动，看不出重试的成效。现改为只统计真正拿到数据的条数：有失败时显示「已查询 17 / 共 300 个关注 · 283 个失败」，每救回一条这个数字就 +1

---

## [0.7.1] - 2026-09-16

### 修复

- **单项刷新 / 批量重试的结果未落盘**：`refresh-one` 只把结果返回给 popup，从不写回 Chrome Storage，关闭 popup 后刷新结果即丢失。现在 background 在刷新成功后同步更新缓存中的对应条目（`patchCachedItem`），失败时保持缓存原样
- **重试后相对时间基准不统一**：相对时间文案只在渲染时计算一次，重试后其它条目仍停留在旧文案，列表里会同时存在多个时间基准。新增 `refreshTimeLabels()`，在「单项刷新成功」「批量重试结束」「全量查询完成」三处统一重算所有条目的时间文案
- **popup 长时间打开时相对时间过时**：新增 60 秒定时校准，开着 popup 时「7 分钟前」会自动走到「12 分钟前」

### 说明

- 单项刷新保持缓存时间戳 `ts` 不变 —— 它表示「上次全量查询时间」，局部刷新不应让它失真

---

## [0.7.0] - 2026-09-16

### 新增

- **内容标题直出**：列表项第二行直接展示最近更新内容的标题（时间 + 标题），不再依赖悬停 tooltip；标题过长时单行省略，悬停可看完整内容
- **失败项批量重试**：统计区的「N 个失败」变为可点击按钮，一键重查全部失败项；「查询失败」分组头同时提供「全部重试」入口。重试过程有真实进度条与剩余时间估算，结束后反馈「N 个成功，M 个仍然失败」
- **侧边栏模式**：新增 `sidePanel` 权限与 `side_panel` 配置，可在浏览器侧边栏中常驻显示；侧边栏模式下布局自适应宽高（宽度 100%、高度 100vh），并隐藏「打开侧边栏」按钮

### 变更

- `upstat` 兜底数据的标题不再带 `[upstat]` 前缀（数据来源已由 `polluted` 字段与「可能不准」标签表达）
- 打开侧边栏后自动关闭 popup，避免两个界面并存

### 兼容性

- 侧边栏模式需要 **Chrome / Edge ≥ 114**；低版本会忽略 `sidePanel` 权限并自动隐藏侧边栏按钮，popup 模式不受影响

### 发布准备

为提交浏览器扩展商店（Microsoft Edge Add-ons）完成以下工作：

- 新增 [PRIVACY.md](PRIVACY.md)：隐私政策，逐项说明数据读取范围、存储位置、网络请求目标与权限用途
- 新增 `scripts/build-zip.js`：零依赖打包脚本，自行写入 ZIP 结构以保证条目路径使用正斜杠。Windows 上常用的 `Compress-Archive` 会写入反斜杠路径，违反 ZIP 规范
- 新增 `docs/EDGE-SUBMISSION.md`（商店提交材料，不随仓库发布）：商店文案、图片规格、给审核员的测试说明、自检清单与常见拒审原因应对
- **启用清单级本地化**：新增 `prototype/_locales/zh_CN/messages.json`，manifest 的 `name` / `description` / `action.default_title` 改用 `__MSG_` 占位符，并声明 `"default_locale": "zh_CN"`。Edge Partner Center 依据 `_locales` 中的消息引用识别可用语言——此前使用硬编码字符串，导致商店列表只出现「英语（美国）」一个语言选项
- 精简 `host_permissions`：移除被 `https://*.bilibili.com/*` 覆盖的 `https://api.bilibili.com/*`
- logo 源素材从 `prototype/icons/` 移至 `assets/`，避免 2.3MB 的非必要文件被打进发布包

---

## [0.6.0] - 2026-09-16

本版本完成 [体验优化调整报告](docs/UX-AUDIT.md) 中「第五批（打磨）」的全部剩余项。至此报告内 **47 项已全部落地**。

### 新增

- **停更分级细化为 6 档**：插入「久未更新 · 1 ~ 3 个月」，形成 活跃 / 偷懒 / 久未更新 / 长期断更 / 疑似退圈 / 基本退圈 六档（外加「查询失败」组），状态色扩展为 6 档灰阶递进
- **单项刷新（F10）**：列表项 hover 显示刷新按钮，可只重查单个 UP 主并就地归入新分组，不影响其它条目
- **导出 CSV 报告（F12）**：header 新增导出按钮，输出含 昵称 / UID / 停更分组 / 最后更新 / 停更天数 / 内容类型 / 最近内容 / 备注，带 BOM 便于 Excel 正确识别
- **清空本地缓存（F14）**：header 新增清空按钮，一键丢弃本地缓存并重置列表（保留 UID 设置）
- **错误码语义化（F11）**：B 站错误码映射为可操作的中文提示（如 `-101 → 登录已过期，请重新登录 B 站`、`-412 → 请求过于频繁，请稍后再试`），原始 code 保留在括号内便于排查
- **加载骨架屏（V18）**：查询启动时以 5 行骨架占位替代纯文字空态，首条结果到达时自动替换
- **深色模式（V13）**：跟随系统明暗自动切换；深色色板按卡片背景 `#1c2027` 重新验算，11 项文本色对比度实测 4.94 ~ 13.79:1，全部达标

### 变更

- **设计 Token 收敛（V8 / V9）**：全站圆角收敛为 `--r-sm / md / lg / pill` 四档；间距全部对齐 4pt 栅格
- **图标资源（V16）**：品牌图标改用 128px 源图（原 48px），高 DPI 下更锐利
- **数字对齐（V17）**：`tabular-nums` 只用于纯数字元素，含中文的时间文案不再启用
- 分组头文案随分级细化同步调整（原「久未更新 · 1 ~ 6 个月」拆为「1 ~ 3 个月」与「3 ~ 6 个月」两档）

### 修复

- 单项刷新失败时不再残留 loading 态

---

## [0.5.0] - 2026-09-16

本版本落地 [体验优化调整报告](docs/UX-AUDIT.md) 中「第四批」的遗留项 V5（停更等级分组头）。

### 新增

- **停更时长分组**：列表改为按停更时长分五组展示，每组带吸顶（sticky）分组头与条目计数

  | 分组 | 范围 | 状态色 |
  | --- | --- | --- |
  | 活跃 | 7 天内 | 绿 `#0d7a5f` |
  | 偷懒 | 7 天 ~ 1 个月 | 棕橙 `#b3541e` |
  | 久未更新 | 1 ~ 6 个月 | 灰 `#6b7587` |
  | 疑似退圈 | 6 个月 ~ 1 年 | 深灰 `#575f6e` |
  | 基本退圈 | 1 年以上 | 更深灰 `#3f4654` |
  | 查询失败 | — | 红 `#c62828` |

- **分档状态色**：状态色由 3 档扩展为 5 档 + 失败档，灰阶递进表示沉寂程度加深，全部通过 WCAG AA 对比度校验
- **分组色继承**：条目的左侧色条与时间文字颜色改为从所属分组继承 CSS 变量，节点跨组移动后自动跟随，无需重建节点

### 变更

- **分组头替代排序按钮**：分组顺序固定为「活跃 → 基本退圈（→ 查询失败）」，组内按最近更新时间倒序
- **搜索框与异常过滤合并到同一行**，header 减少一行（153px）

### 移除

- 移除「最新优先 / 最旧优先」手动排序按钮（分组已按停更时长的自然顺序组织，无需手工排序）
- 移除 `sortDir` 偏好项，`bili_prefs` 现在只保存异常过滤状态

---

## [0.4.0] - 2026-09-16

本版本落地 [体验优化调整报告](docs/UX-AUDIT.md) 中「第三批（P1 交互规范）」I4、I5。

### 变更

- **列表重排优化**：排序 / 过滤 / 搜索切换时不再重建整个列表，改为复用现有 DOM 节点、只移动需要移动的节点；仅在可见性发生变化时才构建或移除对应节点。300 条数据实测排序耗时由「全量重建」降到 **6.3ms**，且 299 个节点对象全部复用，无整屏闪烁
- **查询完成反馈**：进度条不再凭空消失，改为「补满至 100% → 停留约 420ms → 淡出并收起约 220ms → 隐藏」，同时状态区更新为「更新于 刚刚」

### 修复

- 隐藏进度条时同步重置 `aria-valuenow`，避免隐藏后仍残留上一次的进度值

---

## [0.3.0] - 2026-09-16

本版本落地 [体验优化调整报告](docs/UX-AUDIT.md) 中「第二批（P1 易用性）」F2–F8。

### 新增

- **首次使用引导**：未设置 UID 时，空状态直接说明 UID 的获取路径（`space.bilibili.com/` 后面那串数字），并提供「打开 B 站获取 UID」按钮
- **搜索过滤**：header 新增搜索框，按昵称或 UID 实时过滤（120ms 防抖）；无匹配时给出空状态与「清除搜索」入口
- **数据时效展示**：状态区显示「更新于 X 分钟前」，数据超过 24 小时未更新时转为警示色
- **偏好持久化**：排序方向与异常过滤状态写入 `chrome.storage.local`，重新打开 popup 后保持一致

### 变更

- **UID 保存改为一步到位**：按钮文案改为「保存并查询」，修改 UID 后一次点击即完成保存、清缓存与查询，操作步骤从 2 步降为 1 步
- **统计口径明确化**：由含义模糊的「已查 89 / 132」改为「已查询 89 / 共 132 个关注 · 3 个失败」，分母口径与失败数一目了然
- **切换 UID 时清空搜索条件**，避免上一个 UID 的搜索上下文影响新列表
- 点击「保存并查询」时若 UID 未变化，直接执行查询（不再提示「UID 未变化」）

---

## [0.2.0] - 2026-09-16

本版本落地 [体验优化调整报告](docs/UX-AUDIT.md) 中「第一批（P0 正确性）」与「第四批（视觉规范 V1–V7）」。

### 新增

- **批量取关**：列表项支持勾选，底部批量操作条提供「打开主页」「复制 UID」「批量取关」；取关为串行请求（间隔 400ms）以规避风控，并在完成后同步剔除缓存中的对应项
- **二次确认弹层**：批量取关前列出将被取关的 UP 主名单，支持点击遮罩或 Esc 取消
- **停止查询**：查询过程中可随时中断，后台会同时中止在途请求并停止后续批次
- **消息隔离**：`item` / `progress` / `task-state` 推送携带 `mid` 与 `runId`，popup 按当前 UID 过滤
- **请求超时与重试**：单请求 10s 超时；单个 UP 主查询失败自动重试一次；错误文案区分「请求超时」「已取消」
- **空状态兜底**：全部 UP 主查询失败、筛选后为空、查询进行中，均有对应空状态与操作入口，不再出现裸空白
- **统一提示条**：错误与操作反馈统一到 header 下方，可手动关闭；信息类提示 2 秒自动消失
- **无障碍支持**：列表项可 Tab 聚焦并支持 Enter / Space 激活；进度条补齐 `role="progressbar"`；排序与过滤按钮补齐 `aria-pressed`

### 变更

- **消息协议**：`item` 消息 payload 由 `item` 对象改为 `{ mid, runId, item }`；`progress` / `task-state` payload 新增 `mid` / `runId` 字段
- **`start` / `reset` 语义统一**：均表示「启动新任务」，旧任务自动失效，不再返回 `skipped`
- **样式 Token 化**：新增品牌色 / 文本 / 状态 / 字号 / 间距 / 圆角 / 动效 token，全站取值统一
- **图标体系**：所有 emoji 替换为内联 SVG 线条图标，统一 `currentColor`
- **布局**：`#app` 固定为 600px 高度，列表改为在 `main` 内部滚动，header 固定
- **状态信息**：统计信息从 11px 副标题移至独立状态区，权重提升

### 修复

- 修复查询进行中切换 UID 时，旧任务数据串台混入新列表的问题
- 修复查询进行中重复触发刷新导致两个任务并发双跑、产生重复请求与重复项的问题
- 修复查询全部失败时异常项被隐藏、列表空白且用户无法理解的问题
- 修复刷新按钮在部分分支下永久置灰、无法恢复的问题
- 修复所有状态色与标签色对比度低于 WCAG AA 4.5:1 的问题
- 修复内容类型标签与停更分级共用颜色导致的语义冲突
- 修复超长昵称折行破坏列表节奏、左侧高亮条溢出卡片圆角的问题

---

## [0.1.0] - 2026-09-16

首个可用的原型版本。

### 新增

- **关注列表全量拉取**：通过 `SESSDATA` + `buvid3` Cookie 鉴权调用 `/x/relation/followings`，自动翻页拉取全部关注 UP 主
- **最近更新时间查询**：
  - 主路径 `/x/polymer/web-dynamic/v1/feed/space`，取回动态后按 `pub_ts` 自行降序排序，规避置顶内容污染
  - 兜底路径 `/x/space/upstat`，失败降级并标记 `polluted`，UI 显示「⚠可能不准」
- **Popup 列表展示**：按最近更新时间倒序展示，每行包含头像、昵称、时间（X 天前）与内容类型标签
- **停更时长分级**：< 7 天（绿）/ 7~30 天（橙）/ > 30 天（灰）三档颜色标识
- **内容类型标签**：区分 视频 / 专栏 / 动态 / 直播，不同类型独立配色
- **置顶识别**：置顶动态打 `📌置顶` 标记
- **升降序切换**：支持「最新优先 / 最旧优先」一键切换
- **异常过滤**：查询失败的 UP 主默认隐藏，可切换显示并查看具体失败原因
- **UID 切换**：支持查询任意 UID 的关注列表，切换时自动清空旧数据
- **实时进度与 ETA**：查询过程展示进度条与基于实际耗时的动态剩余时间估算
- **任务状态机**：`idle / running / done / error` 四态管理，避免反复开关 popup 触发重复查询
- **Service Worker 保活**：popup 通过长连接（`keepalive` port）避免后台脚本在查询期间被休眠
- **并发与限流控制**：并发度 5、每批间隔 80ms，降低触发风控的概率
- **本地缓存**：查询结果写入 `chrome.storage.local`，并提供缓存优先的渲染路径
- **UI**：白卡片式布局，B 站品牌蓝（`#00aeec`）配色，含空状态、错误态与加载态

### 修复

- **统一缓存键**：`background.js`（原 `'***'`）与 `popup.js` 的 `CACHE_KEY` 统一为 `'bili_follow_cache'`，修复后台写入的缓存无法被 popup 读取、缓存永不命中的问题
- **移除无效的缓存写入**：删除 `popup.js` 中从未被调用的 `saveToCache()` 与未使用的 `CACHE_TTL`，缓存写入职责统一归属 `background.js`
- **统一版本号来源**：源文件头部不再标注版本号，改由 `manifest.json` 作为唯一版本来源
- **补充异常捕获**：`background.js` 中 3 处 `chrome.runtime.sendMessage` 调用补充 `.catch()`，避免 popup 未打开时产生未处理的 Promise 拒绝
- **移除死代码**：删除 `popup.js` 中只写不读的 `currentProgressTotal`
- **移除硬编码 UID**：`DEFAULT_UID` 由作者个人 UID 改为空字符串，首次使用需用户自行填写，并补充未填写 UID 时的空状态提示与刷新拦截

### 文档

- 新增 `PROPOSAL.md` 项目策划书，包含需求分析、正反向视角对比、技术方案与分阶段规划
