# 项目策划书：B 站关注最近更新浏览器插件

> **说明**：本文档是项目立项时的策划书，保留原始需求分析与设计思路。文中「阶段 N」的规划早已执行完毕，部分技术细节也已随版本演进（详见 [README.md](README.md) 与 [CHANGELOG.md](CHANGELOG.md)）；已经过时的实现描述在相应位置做了更正标注。

## 一、需求与痛点

### 1.1 核心洞察：这是一个「反向需求」

B 站本身有一套成熟的内容分发系统，关注列表、动态流、首页推荐——它们都聚焦在 **"哪些 UP 主最近更新了"** 这一正向问题：

- 关注列表按"最近动态"乱序混排视频/专栏/动态
- 首页推荐推流"今天看什么"
- 动态流展示"关注的人最近发布了什么"

**这些系统只解决"找出最近更新"的正向问题，没人解决反向问题——"哪些 UP 主已经很久没更新了"。**

在长期不维护的 B 站关注列表里，几百个 UP 主混在一起，"最近还活跃"和"早就停更退圈"的账号完全没区分，用户根本不知道自己关注列表的真实健康度。

### 1.2 痛点：抓"偷懒摸鱼/退圈"账号的低效

重度 B 站用户（关注 50+ UP 主）面临的实际困难：

| 场景 | 当前困境 | 我们的解决 |
| --- | --- | --- |
| 想知道哪些 UP 主偷懒了 | 需要逐个点进主页翻"最新动态" | 一份按停更时长排序的列表直接给出 |
| 想清理关注列表 | 不知道谁是真退圈、谁是间歇性更新 | 按停更时长分档高亮 + 勾选后批量取关 |
| 看自己关注列表的健康度 | 没数据 | 六档停更分组 + 每组计数，可导出 CSV 报告 |
| 关注了充电专属 UP 主但动态流不显示 | 漏看 | 通过 dynamic feed 兜底识别 |

### 1.3 正向 vs 反向视角对比

| 维度 | B 站原生能力 | 本插件 |
| --- | --- | --- |
| 哪些 UP 主今天更新了 | ✅ 动态流、首页推荐 | 不必要（重复造轮子） |
| 哪些 UP 主 7 天/30 天没更新 | ❌ 无 | ✅ 主动按停更时长排序 |
| 哪些 UP 主可能已退圈（>90 天） | ❌ 隐藏在其他 UP 主中间 | ✅ 高亮标记、一眼可见 |
| 哪些 UP 主只发"充电专属"不出现 | ❌ 普通动态里看不到 | ✅ 可识别（依赖 dynamic feed） |

### 1.4 目标用户

- **核心**：B 站重度用户（关注列表 ≥50 UP 主），长期积累了一批已退圈/偷懒账号，想要**掌控"看什么"的主动权 + 定期清理关注列表**
- **次要**：研究型/数据型用户，想看关注列表整体活跃度统计

---

## 二、目标与设计理念

**这个插件的本质，是把"关注列表"从 B 站视角（"哪些新内容"）转换为用户视角（"我应该保留谁、取关谁"）。**

- 不抢 B 站推荐系统的活（不重复造轮子）
- 解决 B 站没解决的盲区（停更/退圈无信号）
- 提供一种"关注列表体检报告"的视角

---

## 三、核心功能（v1 MVP）

### 3.1 必做（Demo 必含）

| 功能 | 说明 |
|---|---|
| 关注列表获取 | 通过 `SESSDATA` cookie 调 `/x/relation/followings` 拿全部 UP 主 UID + 昵称 |
| 最近更新时间查询 | 从 dynamic feed 取最近 10 条、按 `pub_ts` 真实排序（防置顶污染） |
| Popup 展示 | 点击扩展图标弹出 popup，列表按停更时长分组展示，每行带「X 天前」+ 内容类型标签 |
| 手动刷新按钮 | 点击立即拉一次最新数据；查询中反复开关 popup 不重复触发（任务状态机） |
| 错误兜底 | API 限流/失败时显示**失败原因** + 红色样式 |

### 3.2 必做的"反向使用场景"支撑

| 功能 | 说明 |
|---|---|
| 停更时长分级 | 现状：活跃（< 7 天）/ 偷懒（7 天 ~ 1 个月）/ 久未更新（1 ~ 3 个月）/ 长期断更（3 ~ 6 个月）/ 疑似退圈（6 个月 ~ 1 年）/ 基本退圈（> 1 年）六档，另设「无公开动态」与「查询失败」两个附加组 |
| 类型标签 | 视频/专栏/动态/直播区分；不同类型最近发布时间权重不同 |
| 置顶标记 | 置顶内容单独标注，不污染真实最近时间 |
| 失败原因可见 | 单个 UP 查询失败时显示具体原因，便于排查 |

---

## 四、技术方案

### 4.1 技术栈

- **Chrome 扩展 Manifest V3**
- **原生 JavaScript**（不引框架，保持轻量）
- **Chrome Storage API** 做本地缓存与偏好存储
- **Chrome Cookies API** 读取 `SESSDATA` + `buvid3`
- **Chrome Alarms API** 定时唤醒后台脚本执行「自动更新」（v0.8.3 引入，默认关闭）
- **Chrome Side Panel API** 侧边栏常驻模式（v0.7 引入，Chrome/Edge 114+）

### 4.2 项目结构

```
projects/bili-follow-recency/
├── PROPOSAL.md          # 本策划书
├── README.md            # 安装使用说明
├── PRIVACY.md           # 隐私政策
├── CHANGELOG.md         # 版本更新记录
├── CONTRIBUTING.md      # 贡献指南
├── docs/                # 体验审查报告等文档
├── scripts/             # 打包脚本
└── prototype/           # 插件本体（加载此目录）
    ├── manifest.json    # MV3 配置（popup 与侧边栏双入口）
    ├── _locales/zh_CN/  # 清单级本地化文案
    ├── background.js    # 后台服务（API 调用 / 任务状态机 / 缓存 / 自动化）
    ├── popup.html/.js/.css        # 弹出层 UI 与逻辑
    ├── options.html/.js/.css      # 设置页（反风控 + 自动化）
    └── icons/           # 插件图标
```

> 立项时设想的 `content.js`（内容脚本）与 `docs/api-research.md` 最终并未采用：当前版本不需要向 B 站页面注入内容，接口调研结论直接沉淀在 [README](README.md#依赖的-b-站接口) 中。

### 4.3 关键 B 站 API

| API | 用途 | 鉴权 |
|---|---|---|
| `https://api.bilibili.com/x/relation/followings?vmid={uid}&pn=1&ps=50` | 分页拿关注列表 | 需要 `SESSDATA` + `buvid3` |
| `https://api.bilibili.com/x/polymer/web-dynamic/v1/feed/space?host_mid={uid}` | UP 主最近动态（防置顶污染的主路径） | 需要 `SESSDATA` + `buvid3` |
| `https://api.bilibili.com/x/space/upstat?mid={uid}` | UP 主最新投稿时间（fallback，可能被置顶污染） | 需要 `SESSDATA` + `buvid3` |
| `https://api.bilibili.com/x/frontend/finger/spi` | 申请 `buvid4` 设备标识（缺少时申请一次并长期缓存） | 同上 |
| `https://api.bilibili.com/x/relation/modify` | 取消关注（`POST`，`act=2`） | 同上 + `bili_jct` |

> `User-Agent` / `Referer` / `Origin` 属浏览器的禁止修改头，JS 设置会被静默忽略，代码中不再尝试设置（v0.8.0 起）。
> 昵称与头像直接取自关注列表接口，立项时设想过的 `/x/space/acc/info` 并未使用。

### 4.4 关键风险与对策

| 风险 | 后果 | 对策 |
|---|---|---|
| API 限流（约 1-2 次/秒） | 关注 200 个 UP 需 100 秒 | popup 加载时显示进度条，逐个请求 + 失败重试 |
| 关注列表分页（每页 50） | 200 UP 主要分 4 页 | 循环翻页直到 `total` 字段耗尽 |
| 置顶视频污染 upstat 时间 | UP 主真实最近更新被覆盖为老视频时间 | 主路径用 dynamic feed 自己按 pub_ts 排；upstat 只作 fallback 并打「可能不准」标记 |
| 接口被改/下线 | 插件失效 | 错误码统一捕获并翻译成中文提示，必要时重试；持续失败可开启「自动重新查询失败项」 |
| SESSDATA 过期 | API 返回 -101 未登录 | 拦截返回码并提示用户重新登录 B 站 |
| CORS 跨域 | 浏览器拒绝请求 | manifest.json 配置 `host_permissions: ["https://*.bilibili.com/*"]` |
| popup 反复打开导致重复查询 | 同一会话内多次触发全量查询 | background 维护任务状态机（idle/running/done），popup 打开只"加入监听"不重复触发 |
| 风控（`-352` 等）导致部分条目失败 | 列表里出现一堆「查询失败」 | v0.8 起提供反风控设置（拟人化节奏 / 并发数 / 熔断冷却 / 设备指纹）；v0.8.3 起可自动重查失败项直到归零 |
| MV3 后台脚本被回收 | 定时任务不执行 | 自动更新改用 `chrome.alarms` 定时唤醒，而不是 `setInterval` |

---

## 五、实施计划

### 阶段 1：Demo MVP（已完成）

- [x] 确定查询对象：使用用户自己的 B 站 UID（后续版本已移除内置 UID，改为首次使用时由用户填写）
- [x] 创建项目目录 + 基础文件结构
- [x] 实现 manifest.json + popup.html + popup.js + background.js
- [x] 跑通"获取关注列表 → 展示最近更新时间"主链路
- [x] 修置顶污染：改用 dynamic feed 自排序
- [x] 修重复触发：任务状态机
- [x] UI 美化（白色卡片化设计）
- [x] 提供 zip 包便于本地加载测试（`scripts/build-zip.js`）
- [x] 根据反馈调整

### 阶段 2：反向场景增强（已完成）

- [x] 按停更时长分组（最终细化为六档 + 两个附加组）
- [x] 长期未更新高亮标记（并入分组状态色）
- [x] 选中多个停更 UP 主 → 一键批量取关
- [x] 后台定时轮询（v0.8.3 落地为「自动更新」：`chrome.alarms` 驱动，频率 5 ~ 1440 分钟可调，默认关闭）
- [ ] B 站关注页面注入时间列（仍未实现，见下方 v0.9 规划）

### 阶段 3：体验优化版（部分完成）

- [x] 导出关注列表活跃度报告（CSV，v0.6）
- [ ] 关注列表健康度仪表盘（活跃度饼图）
- [ ] 关注/取关变化检测
- [ ] 跨浏览器支持（Firefox）

---

## 六、验收标准

**阶段 1 完成标志**（用户侧可操作）：
1. 用户在 Chrome 加载扩展成功，图标出现在工具栏
2. 点击图标弹出 popup，显示一个列表
3. 列表内容是用户实际关注的 UP 主，每行有"X 天前更新"
4. 列表按停更时长分组展示，组内按最近更新时间倒序（**最近更新的在最上面**）
5. 点击"刷新"按钮可重新拉数据
6. 反复开关 popup 不重复触发查询
7. UI 干净美观，白色卡片化

**失败兜底**：
- 未登录（SESSDATA 缺失）→ popup 显示"请先登录 B 站"
- API 限流/失败 → 显示进度 + 失败项标记
- 单个 UP 失败 → 显示具体失败原因

---

## 七、未来扩展（v2+ 候选）

### 7.1 反向场景深化（优先级高，与本插件定位强相关）

- ✅ **批量取关**：选中 N 个停更 UP 主 → 一键批量取关（v0.2 已实现）
- ✅ **按停更时长分组**：活跃 / 偷懒 / 久未更新 / 长期断更 / 疑似退圈 / 基本退圈六档（v0.5 ~ v0.6 已实现，另有「无公开动态」「查询失败」两个附加组）
- ⬜ **退圈预警**：长期不更新 + 之前高频发视频的 UP 主 → 标记"可能退圈"高亮（尚未实现）
- ⬜ **一键跳转到最新**：点行直接跳该 UP 主最新动态（当前是打开其主页）

### 7.2 正向体验（次优）

- ✅ 展示「最新动态类型」（视频/专栏/动态/直播）
- ✅ 侧边栏常驻、深色模式、CSV 导出（v0.6 / v0.7 已实现）
- ✅ 后台定时轮询（v0.8.3 已实现「自动更新」，可按 5 ~ 1440 分钟设定频率）
- ⬜ 通知推送（关注的 UP 主更新时桌面通知）
- ⬜ 跨浏览器支持（Firefox）
- ⬜ 数据可视化（关注列表活跃度仪表盘）

---

## 八、设计原则（备忘）

1. **不抢 B 站的活**——B 站已经做好的"最近更新"展示我们不重复造轮子
2. **解决 B 站没解决的盲区**——停更/退圈无信号是 B 站官方盲区，正是本插件价值所在
3. **可控可恢复**——所有本地缓存、刷新按钮都让用户掌控
4. **简洁优先**——只做最核心的反向场景展示

---

*本文档保留立项时的原始策划内容，已完成 / 未完成项已在上文标注。当前版本的功能、参数与实现细节，以 [README.md](README.md) 与 [CHANGELOG.md](CHANGELOG.md) 为准。*